{% extends "adminLayout.html.twig" %}
{% import "macros.html.twig" as macros %}
{% import "crudMacros.html.twig" as crudMacros %}

{% set menu = 'SOURCES' %}

{% block content %}

{{ macros.Title('Fuentes') }}

{{ include('tabbedContentMenu.html.twig') }}
<div class="contenedorAdmin">
    {{ crudMacros.addValidateFormJs() }}

     {% if source.Caption %}
        {{ macros.subTitle(source.Caption) }}
	 {% else %}
        {{ macros.subTitle('Nueva fuente') }}
     {% endif %}
    <form onsubmit="return validateFormJs();" action="/admin/sourcesItem?id={{ id }}" method="post">
    {{ crudMacros.ShowSourceFields(source, '', institutions, true, possibleTypes) }}
	<div class='dBlockNewLineS3'><br></div>
	{{ crudMacros.ShowButtonsRow() }}
        </form>
</div>

{% endblock %}
